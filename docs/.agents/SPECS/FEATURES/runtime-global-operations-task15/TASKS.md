# TASKS — Runtime Global Operations (Task 15)

- Implement RuntimeOperation types and manager-owned operation store.
- Add lifecycle wrappers in RuntimeManager with progress/status updates.
- Add operation endpoints in runtime API.
- Add cancellation pathway for running operations.
- Update frontend types/hooks/components to render operation-driven phases.
- Validate with two browser tabs + refresh persistence behavior.
